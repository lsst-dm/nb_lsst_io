[tox]
envlist =
    sphinx
isolated_build = True

[testenv]
basepython = python
skip_install = True
deps =
    -rrequirements.txt

[testenv:sphinx]
commands =
    sphinx-build -b html -d _build/doctrees -n -W . _build/html

[testenv:linkcheck]
commands =
    sphinx-build -b linkcheck -d _build/doctrees -n -W . _build/linkcheck
